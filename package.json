{
  "name": "release-guard-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "encrypt": "staticrypt dist/index.html -p ${PASSWORD:-1234} -o dist/index.html --template \"encrypt_template.html\""
  },
  "devDependencies": {
    "staticrypt": "^3.0.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "marked": "^11.0.0",
    "github-markdown-css": "^5.5.0"
  }
}
